import axios from "axios";

console.log("API Base URL:", import.meta.env.VITE_API_BASE);

const api = axios.create({
  baseURL: import.meta.env.VITE_API_BASE, // no fallback
  withCredentials: true,
});

export default api;
